# <!-- Managed by the Dictum CLI. -->

interface:
  display_name: "Dictum"
  short_description: "Polish a draft before Codex acts"
  default_prompt: "Use $dictum to turn this rough draft into an execution-ready prompt and wait for my approval."
